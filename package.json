{
  "name": "bikemap",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:icons": "bash scripts/build_pwa_assets.sh",
    "build:fonts": "bash scripts/fetch_material_symbols.sh",
    "build:manifest": "python3 scripts/build_data_manifest.py",
    "build:pwa": "npm run build:fonts && npm run build:icons && npm run build:manifest"
  },
  "dependencies": {
    "@protomaps/basemaps": "^5.7.2",
    "flexsearch": "^0.8.212",
    "maplibre-gl": "^5.24.0",
    "pmtiles": "^4.4.1"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
